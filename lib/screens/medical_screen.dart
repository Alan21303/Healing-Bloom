import 'package:flutter/material.dart';

class MedicalAnalysisScreen extends StatelessWidget {
  const MedicalAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.white);
  }
}
