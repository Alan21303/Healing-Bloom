import 'package:flutter/material.dart';

class MedicinePredictionScreen extends StatelessWidget {
  const MedicinePredictionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Medicine Prediction')),
      body: const Center(child: Text('Medicine Prediction Page')),
    );
  }
}
